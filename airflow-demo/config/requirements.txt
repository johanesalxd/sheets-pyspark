# Python dependencies for notebook execution in Vertex AI
# These match the dependencies in the root pyproject.toml

gspread>=5.0.0
pandas>=2.0.0
oauth2client>=4.1.3
bigframes>=1.0.0
db-dtypes>=1.0.0

# Additional dependencies for notebook execution
ipykernel>=6.0.0
papermill>=2.4.0
